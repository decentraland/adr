## Outfits Static

```

1. Fail if pointer does not have the structure "${ethAddress}:outfits".

2. Fail if outfit slots are repeated.

3. Fail if size of 'namesForExtraSlots' is different from the actual number of extra outfits. An extra slot is such that 4 < extraSlot < 10.
```
